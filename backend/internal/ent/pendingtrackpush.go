// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/technobecet/tsundoku/internal/ent/pendingtrackpush"
)

// PendingTrackPush is the model entity for the PendingTrackPush schema.
type PendingTrackPush struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TrackBindingID holds the value of the "track_binding_id" field.
	TrackBindingID uuid.UUID `json:"track_binding_id,omitempty"`
	// Chapter holds the value of the "chapter" field.
	Chapter float64 `json:"chapter,omitempty"`
	// Attempts holds the value of the "attempts" field.
	Attempts int `json:"attempts,omitempty"`
	// NextAttemptAt holds the value of the "next_attempt_at" field.
	NextAttemptAt *time.Time `json:"next_attempt_at,omitempty"`
	// LastError holds the value of the "last_error" field.
	LastError string `json:"last_error,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PendingTrackPush) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pendingtrackpush.FieldChapter:
			values[i] = new(sql.NullFloat64)
		case pendingtrackpush.FieldAttempts:
			values[i] = new(sql.NullInt64)
		case pendingtrackpush.FieldLastError:
			values[i] = new(sql.NullString)
		case pendingtrackpush.FieldNextAttemptAt, pendingtrackpush.FieldCreatedAt, pendingtrackpush.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case pendingtrackpush.FieldID, pendingtrackpush.FieldTrackBindingID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PendingTrackPush fields.
func (_m *PendingTrackPush) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pendingtrackpush.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case pendingtrackpush.FieldTrackBindingID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field track_binding_id", values[i])
			} else if value != nil {
				_m.TrackBindingID = *value
			}
		case pendingtrackpush.FieldChapter:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field chapter", values[i])
			} else if value.Valid {
				_m.Chapter = value.Float64
			}
		case pendingtrackpush.FieldAttempts:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field attempts", values[i])
			} else if value.Valid {
				_m.Attempts = int(value.Int64)
			}
		case pendingtrackpush.FieldNextAttemptAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field next_attempt_at", values[i])
			} else if value.Valid {
				_m.NextAttemptAt = new(time.Time)
				*_m.NextAttemptAt = value.Time
			}
		case pendingtrackpush.FieldLastError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_error", values[i])
			} else if value.Valid {
				_m.LastError = value.String
			}
		case pendingtrackpush.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case pendingtrackpush.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PendingTrackPush.
// This includes values selected through modifiers, order, etc.
func (_m *PendingTrackPush) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this PendingTrackPush.
// Note that you need to call PendingTrackPush.Unwrap() before calling this method if this PendingTrackPush
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PendingTrackPush) Update() *PendingTrackPushUpdateOne {
	return NewPendingTrackPushClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PendingTrackPush entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PendingTrackPush) Unwrap() *PendingTrackPush {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: PendingTrackPush is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PendingTrackPush) String() string {
	var builder strings.Builder
	builder.WriteString("PendingTrackPush(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("track_binding_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TrackBindingID))
	builder.WriteString(", ")
	builder.WriteString("chapter=")
	builder.WriteString(fmt.Sprintf("%v", _m.Chapter))
	builder.WriteString(", ")
	builder.WriteString("attempts=")
	builder.WriteString(fmt.Sprintf("%v", _m.Attempts))
	builder.WriteString(", ")
	if v := _m.NextAttemptAt; v != nil {
		builder.WriteString("next_attempt_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("last_error=")
	builder.WriteString(_m.LastError)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PendingTrackPushes is a parsable slice of PendingTrackPush.
type PendingTrackPushes []*PendingTrackPush
