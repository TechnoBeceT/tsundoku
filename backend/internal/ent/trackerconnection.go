// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/technobecet/tsundoku/internal/ent/trackerconnection"
)

// TrackerConnection is the model entity for the TrackerConnection schema.
type TrackerConnection struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TrackerID holds the value of the "tracker_id" field.
	TrackerID int `json:"tracker_id,omitempty"`
	// AccessToken holds the value of the "access_token" field.
	AccessToken string `json:"-"`
	// RefreshToken holds the value of the "refresh_token" field.
	RefreshToken string `json:"-"`
	// TokenType holds the value of the "token_type" field.
	TokenType string `json:"token_type,omitempty"`
	// ExpiresAt holds the value of the "expires_at" field.
	ExpiresAt *time.Time `json:"expires_at,omitempty"`
	// Username holds the value of the "username" field.
	Username string `json:"username,omitempty"`
	// Password holds the value of the "password" field.
	Password string `json:"-"`
	// ScoreFormat holds the value of the "score_format" field.
	ScoreFormat string `json:"score_format,omitempty"`
	// TokenExpired holds the value of the "token_expired" field.
	TokenExpired bool `json:"token_expired,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TrackerConnection) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case trackerconnection.FieldTokenExpired:
			values[i] = new(sql.NullBool)
		case trackerconnection.FieldTrackerID:
			values[i] = new(sql.NullInt64)
		case trackerconnection.FieldAccessToken, trackerconnection.FieldRefreshToken, trackerconnection.FieldTokenType, trackerconnection.FieldUsername, trackerconnection.FieldPassword, trackerconnection.FieldScoreFormat:
			values[i] = new(sql.NullString)
		case trackerconnection.FieldExpiresAt, trackerconnection.FieldCreatedAt, trackerconnection.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case trackerconnection.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TrackerConnection fields.
func (_m *TrackerConnection) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case trackerconnection.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case trackerconnection.FieldTrackerID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field tracker_id", values[i])
			} else if value.Valid {
				_m.TrackerID = int(value.Int64)
			}
		case trackerconnection.FieldAccessToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field access_token", values[i])
			} else if value.Valid {
				_m.AccessToken = value.String
			}
		case trackerconnection.FieldRefreshToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field refresh_token", values[i])
			} else if value.Valid {
				_m.RefreshToken = value.String
			}
		case trackerconnection.FieldTokenType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token_type", values[i])
			} else if value.Valid {
				_m.TokenType = value.String
			}
		case trackerconnection.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = new(time.Time)
				*_m.ExpiresAt = value.Time
			}
		case trackerconnection.FieldUsername:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field username", values[i])
			} else if value.Valid {
				_m.Username = value.String
			}
		case trackerconnection.FieldPassword:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password", values[i])
			} else if value.Valid {
				_m.Password = value.String
			}
		case trackerconnection.FieldScoreFormat:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field score_format", values[i])
			} else if value.Valid {
				_m.ScoreFormat = value.String
			}
		case trackerconnection.FieldTokenExpired:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field token_expired", values[i])
			} else if value.Valid {
				_m.TokenExpired = value.Bool
			}
		case trackerconnection.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case trackerconnection.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TrackerConnection.
// This includes values selected through modifiers, order, etc.
func (_m *TrackerConnection) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this TrackerConnection.
// Note that you need to call TrackerConnection.Unwrap() before calling this method if this TrackerConnection
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TrackerConnection) Update() *TrackerConnectionUpdateOne {
	return NewTrackerConnectionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TrackerConnection entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TrackerConnection) Unwrap() *TrackerConnection {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TrackerConnection is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TrackerConnection) String() string {
	var builder strings.Builder
	builder.WriteString("TrackerConnection(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tracker_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TrackerID))
	builder.WriteString(", ")
	builder.WriteString("access_token=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("refresh_token=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("token_type=")
	builder.WriteString(_m.TokenType)
	builder.WriteString(", ")
	if v := _m.ExpiresAt; v != nil {
		builder.WriteString("expires_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("username=")
	builder.WriteString(_m.Username)
	builder.WriteString(", ")
	builder.WriteString("password=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("score_format=")
	builder.WriteString(_m.ScoreFormat)
	builder.WriteString(", ")
	builder.WriteString("token_expired=")
	builder.WriteString(fmt.Sprintf("%v", _m.TokenExpired))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TrackerConnections is a parsable slice of TrackerConnection.
type TrackerConnections []*TrackerConnection
