// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/technobecet/tsundoku/internal/ent/seriesprovider"
	"github.com/technobecet/tsundoku/internal/ent/suwayomisyncstate"
)

// SuwayomiSyncState is the model entity for the SuwayomiSyncState schema.
type SuwayomiSyncState struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// SeriesProviderID holds the value of the "series_provider_id" field.
	SeriesProviderID uuid.UUID `json:"series_provider_id,omitempty"`
	// LastSyncedAt holds the value of the "last_synced_at" field.
	LastSyncedAt *time.Time `json:"last_synced_at,omitempty"`
	// LastError holds the value of the "last_error" field.
	LastError string `json:"last_error,omitempty"`
	// State holds the value of the "state" field.
	State string `json:"state,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SuwayomiSyncStateQuery when eager-loading is set.
	Edges        SuwayomiSyncStateEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SuwayomiSyncStateEdges holds the relations/edges for other nodes in the graph.
type SuwayomiSyncStateEdges struct {
	// SeriesProvider holds the value of the series_provider edge.
	SeriesProvider *SeriesProvider `json:"series_provider,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SeriesProviderOrErr returns the SeriesProvider value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SuwayomiSyncStateEdges) SeriesProviderOrErr() (*SeriesProvider, error) {
	if e.SeriesProvider != nil {
		return e.SeriesProvider, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: seriesprovider.Label}
	}
	return nil, &NotLoadedError{edge: "series_provider"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SuwayomiSyncState) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case suwayomisyncstate.FieldLastError, suwayomisyncstate.FieldState:
			values[i] = new(sql.NullString)
		case suwayomisyncstate.FieldLastSyncedAt:
			values[i] = new(sql.NullTime)
		case suwayomisyncstate.FieldID, suwayomisyncstate.FieldSeriesProviderID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SuwayomiSyncState fields.
func (_m *SuwayomiSyncState) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case suwayomisyncstate.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case suwayomisyncstate.FieldSeriesProviderID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field series_provider_id", values[i])
			} else if value != nil {
				_m.SeriesProviderID = *value
			}
		case suwayomisyncstate.FieldLastSyncedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_synced_at", values[i])
			} else if value.Valid {
				_m.LastSyncedAt = new(time.Time)
				*_m.LastSyncedAt = value.Time
			}
		case suwayomisyncstate.FieldLastError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_error", values[i])
			} else if value.Valid {
				_m.LastError = value.String
			}
		case suwayomisyncstate.FieldState:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field state", values[i])
			} else if value.Valid {
				_m.State = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SuwayomiSyncState.
// This includes values selected through modifiers, order, etc.
func (_m *SuwayomiSyncState) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySeriesProvider queries the "series_provider" edge of the SuwayomiSyncState entity.
func (_m *SuwayomiSyncState) QuerySeriesProvider() *SeriesProviderQuery {
	return NewSuwayomiSyncStateClient(_m.config).QuerySeriesProvider(_m)
}

// Update returns a builder for updating this SuwayomiSyncState.
// Note that you need to call SuwayomiSyncState.Unwrap() before calling this method if this SuwayomiSyncState
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SuwayomiSyncState) Update() *SuwayomiSyncStateUpdateOne {
	return NewSuwayomiSyncStateClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SuwayomiSyncState entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SuwayomiSyncState) Unwrap() *SuwayomiSyncState {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SuwayomiSyncState is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SuwayomiSyncState) String() string {
	var builder strings.Builder
	builder.WriteString("SuwayomiSyncState(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("series_provider_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SeriesProviderID))
	builder.WriteString(", ")
	if v := _m.LastSyncedAt; v != nil {
		builder.WriteString("last_synced_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("last_error=")
	builder.WriteString(_m.LastError)
	builder.WriteString(", ")
	builder.WriteString("state=")
	builder.WriteString(_m.State)
	builder.WriteByte(')')
	return builder.String()
}

// SuwayomiSyncStates is a parsable slice of SuwayomiSyncState.
type SuwayomiSyncStates []*SuwayomiSyncState
